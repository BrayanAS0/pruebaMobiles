name: Build in OpenShift on Push

on:
  push:
    branches:
      - main

jobs:
  trigger-build:
    runs-on: ubuntu-latest

    steps:
      - name: 🔁 Disparar webhook de OpenShift
        run: |
          curl -X POST \
            -H "Content-Type: application/json" \
            https://api.rm1.0a51.p1.openshiftapps.com:6443/apis/build.openshift.io/v1/namespaces/brayan3312-dev/buildconfigs/prueba-mobiles-git/webhooks/09eb7a46d787cb4c/generic
